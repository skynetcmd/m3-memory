"""Dashboard package: extracted from bin/dashboard_server.py.

Holds the HTML/CSS template constants (templates.py) so the server module is
logic, not ~1700 lines of inline markup. Behavior-preserving extraction.
"""
