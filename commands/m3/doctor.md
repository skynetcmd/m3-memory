---
name: m3:doctor
description: Health check m3-memory — package version, installed payload, chatlog DB row count, per-agent hook state.
---

Run `mcp-memory doctor` and summarize:

```
!`mcp-memory doctor`
```

If anything reads `[X]` or shows `(never)` for last capture when you'd expect captures, explain the likely cause and offer a fix command.
