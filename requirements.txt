# M3 Max Agentic OS — Unified Dependencies
# Install: pip install -r requirements.txt
# Versions pinned to major.minor.* for supply-chain safety (2026-04-05)

# --- Core Engine & MCP ---
fastmcp>=3.2,<4
mcp>=1.3,<2

# --- Network & HTTP ---
httpx[http2]>=0.28,<1
h2>=4.3.0,<5
requests>=2.33,<3
urllib3>=2.3,<3

# --- Database & Storage ---
psycopg2-binary>=2.9,<3
psycopg[binary]>=3.2,<4
chromadb-client>=0.6,<1
sqlglot>=26.1,<27

# --- Security & Auth ---
cryptography>=46.0,<47
keyring>=25.7,<26
pywin32>=308; sys_platform == 'win32'

# --- System & Thermal ---
psutil>=7.2,<8
pyobjc-framework-CoreWLAN; sys_platform == 'darwin'

# --- Intelligence & Vector ---
numpy>=1.22.5,<2.0.0
scipy>=1.15,<2
tqdm>=4.67,<5

# --- Utilities ---
python-dotenv>=1.2,<2
PyYAML>=6.0,<7
fpdf2>=2.8,<3
beautifulsoup4>=4.14,<5
argparse>=1.4,<2

# --- MCP Proxy & API ---
fastapi>=0.135,<1
uvicorn>=0.43,<1

# --- Development & Testing ---
pytest>=9.0,<10
pytest-asyncio>=1.3,<2
ruff>=0.9,<1
mypy>=1.15,<2
types-requests
types-setuptools
